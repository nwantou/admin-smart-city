
  # Municipal Dashboard App

  This is a code bundle for Municipal Dashboard App. The original project is available at https://www.figma.com/design/eTp3fNxhz1S3uje0Do07xx/Municipal-Dashboard-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  